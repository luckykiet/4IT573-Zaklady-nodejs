<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title %></title>
	</head>
	<body>
		<h1><%= title %></h1>

		<ul>
			<% for (const todo of todos) { %>
			<li>
				<a href="/todo/<%= todo.id %>"> <%= todo.title %> </a>
				&nbsp; <% if(todo.priority === 'low') { %>
				<span style="color: green">Nizka</span>
				<% } else if(todo.priority === 'medium') {%>
				<span style="color: orange">Stredni</span>
				<% } else if(todo.priority === 'high') {%>
				<span style="color: red">Vysoka</span>
				<% }else { %>
				<span><%= todo.priority %></span>
				<% } %> &nbsp;
				<a href="/remove-todo/<%= todo.id %>"> odebrat </a>
				&nbsp;
				<a href="/toggle-todo/<%= todo.id %>">
					<%= todo.done ? 'hotovo' : 'nehotvo' %>
				</a>
			</li>
			<% } %>
		</ul>

		<form method="post" action="/add-todo">
			<input placeholder="Název todočka" name="title" />
			<button type="submit">Přidat</button>
		</form>
	</body>
</html>
